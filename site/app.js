const routes={19:{label:'BLOCK 19 · WORKER ROUTE',title:'Holocene rules, isolated',copy:'The host binds the request to block 19’s exact parent and sends complete-block execution to the approved historical worker.',executor:'Pinned worker',state:'Immutable parent view'},20:{label:'BLOCK 20 · LOCAL ROUTE',title:'Isthmus activates here',copy:'Block 20 has a pre-Isthmus parent. The real activation injects upgrade deposits and changes commitment rules; current code executes it in-process.',executor:'Current host',state:'Canonical state'},21:{label:'BLOCK 21 · LOCAL ROUTE',title:'Current rules continue',copy:'With Isthmus active, block 21 remains on the host’s current execution path. The host still owns validation, forkchoice and persistence.',executor:'Current host',state:'Canonical state'}};
document.querySelectorAll('.block').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.block').forEach(item=>{item.classList.toggle('active',item===button);item.setAttribute('aria-pressed',item===button)});const route=routes[button.dataset.block];document.querySelector('#route-label').textContent=route.label;document.querySelector('#route-title').textContent=route.title;document.querySelector('#route-copy').textContent=route.copy;document.querySelector('#route-executor').textContent=route.executor;document.querySelector('#route-state').textContent=route.state}));
document.querySelector('#copy').addEventListener('click',async event=>{try{await navigator.clipboard.writeText(document.querySelector('#commands').textContent);event.currentTarget.textContent='Copied'}catch{event.currentTarget.textContent='Select + copy'}setTimeout(()=>event.currentTarget.textContent='Copy commands',1800)});
fetch('./data/evidence.json').then(response=>{if(!response.ok)throw new Error('Evidence unavailable');return response.json()}).then(data=>{document.querySelector('#evidence-label').textContent=data.provenance.label;document.querySelector('#evidence-date').textContent=`Final local run · ${data.provenance.date}`;document.querySelector('#checkout-status').textContent=data.provenance.checkoutStatus;document.querySelector('#stats').innerHTML=data.checks.map(check=>`<div class="stat"><b>${check.value}</b><span>${check.label}</span><small>${check.detail}</small></div>`).join('');document.querySelector('#worker-latency').textContent=`${data.latency.workerMs} ms`;document.querySelector('#reference-latency').textContent=`${data.latency.referenceMs} ms`;document.querySelector('#latency-note').textContent=data.latency.note}).catch(()=>document.querySelector('#stats').innerHTML='<p>Evidence data unavailable. Serve the site over HTTP to load it.</p>');
